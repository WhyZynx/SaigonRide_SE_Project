@model List<SaigonRideProject.ViewModels.RentalHistoryViewModel>
<div class="container mt-4">

    <h2>Trip History</h2>

    <table class="table table-striped mt-3">

        <thead>
            <tr>
                <th>Vehicle Type</th>
                <th>Plate Number</th>
                <th>Start</th>
                <th>End</th>
                <th>Base</th>
                <th>Discount</th>
                <th>Total</th>
                <th>Payment</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var r in Model)
            {
                <tr>
                    <td>@r.VehicleType</td>
                    <td>@r.PlateNumber</td>
                    <td>@r.StartTime</td>
                    <td>@r.EndTime</td>
                    <td>@r.BaseAmount.ToString("N0")</td>
                    <td>@(r.DiscountPercent * 100)%</td>
                    <td><b>@r.FinalAmount.ToString("N0")</b></td>
                    <td>@r.PaymentMethod</td>
                </tr>
            }

        </tbody>

    </table>

</div>